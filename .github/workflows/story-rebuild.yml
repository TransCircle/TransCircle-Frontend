name: Retired Story Rebuild

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The legacy story/ static export is retired."
          echo "Use the Deploy Frontend workflow to publish the v3 Worker application."
